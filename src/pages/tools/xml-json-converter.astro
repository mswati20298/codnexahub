---
import ToolLayout from '../../layouts/ToolLayout.astro';
import XmlJsonConverter from '../../components/XmlJsonConverter.jsx';
---
<ToolLayout title="XML ↔ JSON Converter" description="Convert between XML and JSON in both directions, using your browser's built-in XML parser.">
  <XmlJsonConverter client:load slot="tool" />
</ToolLayout>
